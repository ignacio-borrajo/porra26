Django>=5.0,<5.2
# Postgres (Railway). psycopg[binary] no requiere libpq local: trae wheels.
psycopg[binary]>=3.2
dj-database-url>=2.2
# Servidor WSGI en producción (Railway corre el Procfile).
gunicorn>=22.0
# Servir estáticos desde el propio gunicorn sin nginx delante.
whitenoise>=6.7
django-axes>=6.4
django-auditlog>=3.0
django-csp>=3.8
python-dotenv>=1.0
Pillow>=10.0
reportlab>=4.0,<5
openpyxl>=3.1
# Parseo de User-Agent para etiquetar dispositivos en UserSession.
user-agents>=2.2
