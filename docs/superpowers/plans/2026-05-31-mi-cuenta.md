# Mi cuenta Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir la página `/mi-cuenta/` con tres tarjetas (datos, seguridad, preferencias) y enlazarla desde el avatar del topbar, según el spec `docs/superpowers/specs/2026-05-31-mi-cuenta-design.md`.

**Architecture:** Una sola URL bajo la app `accounts`. Una `MyAccountView` (LoginRequiredMixin + View) que despacha el POST por el valor de `action` y delega en dos forms: un `ProfileForm` nuevo (ModelForm sobre `User` con cuatro campos editables) y el `ChangePasswordForm` existente. Template server-rendered con tres `<form>` independientes. Auditoría vía `AuditLog`.

**Tech Stack:** Django 4/5, plantillas Django, pytest + pytest-django, factory-boy. Sin JS adicional.

---

## File Structure

- **Modificar** `accounts/forms.py` — añadir `ProfileForm`.
- **Modificar** `accounts/views.py` — añadir `MyAccountView`.
- **Modificar** `accounts/urls.py` — añadir ruta `mi-cuenta/`.
- **Crear** `templates/accounts/my_account.html` — página con tres tarjetas.
- **Modificar** `templates/partials/_topbar.html` — envolver bloque del avatar en `<a>`.
- **Crear** `accounts/tests/test_my_account.py` — suite completa.

Convenciones del proyecto verificadas:
- Tests con `pytest.mark.django_db` (también hay `conftest.py` con autouse `db`, así que la marca es redundante pero no daña).
- Factorías en `accounts/tests/factories.py`: `UserFactory` con `is_jugador=True`, `must_change_password=False`. Sobreescribir `password` con kwarg.
- Modelo `User`: campos editables `name`, `dept`, `sede`, `puesto` (todos con `choices` excepto `name`).
- Mensajes Django ya se renderizan globalmente vía `templates/partials/_toast.html` incluido en `base.html`.

---

## Task 1: ProfileForm (TDD)

**Files:**
- Modify: `accounts/forms.py`
- Create: `accounts/tests/test_my_account.py`

- [ ] **Step 1.1: Escribir tests del ProfileForm**

Añade al final de `accounts/tests/test_my_account.py` (crea el archivo si no existe; deja una línea en blanco al final):

```python
import pytest
from django.urls import reverse

from accounts.forms import ProfileForm
from accounts.models import AuditLog, User
from accounts.tests.factories import UserFactory


@pytest.mark.django_db
def test_profile_form_saves_editable_fields():
    user = UserFactory(name="Ana", dept="", sede="", puesto="")
    form = ProfileForm(
        data={"name": "Ana López", "dept": "gestion", "sede": "vigo", "puesto": "desarrollo"},
        instance=user,
    )
    assert form.is_valid(), form.errors
    saved = form.save()
    saved.refresh_from_db()
    assert saved.name == "Ana López"
    assert saved.dept == "gestion"
    assert saved.sede == "vigo"
    assert saved.puesto == "desarrollo"


@pytest.mark.django_db
def test_profile_form_strips_name():
    user = UserFactory()
    form = ProfileForm(
        data={"name": "  Ana  ", "dept": "gestion", "sede": "vigo", "puesto": "desarrollo"},
        instance=user,
    )
    assert form.is_valid(), form.errors
    assert form.cleaned_data["name"] == "Ana"


@pytest.mark.django_db
def test_profile_form_rejects_empty_name():
    user = UserFactory()
    form = ProfileForm(
        data={"name": "   ", "dept": "gestion", "sede": "vigo", "puesto": "desarrollo"},
        instance=user,
    )
    assert not form.is_valid()
    assert "name" in form.errors


@pytest.mark.django_db
def test_profile_form_rejects_invalid_choice():
    user = UserFactory()
    form = ProfileForm(
        data={"name": "Ana", "dept": "INEXISTENTE", "sede": "vigo", "puesto": "desarrollo"},
        instance=user,
    )
    assert not form.is_valid()
    assert "dept" in form.errors


@pytest.mark.django_db
def test_profile_form_ignores_uneditable_fields():
    user = UserFactory(email="ana@edisa.com", is_gestor=False, is_staff=False)
    form = ProfileForm(
        data={
            "name": "Ana",
            "dept": "gestion",
            "sede": "vigo",
            "puesto": "desarrollo",
            "email": "otro@edisa.com",
            "is_gestor": "on",
            "is_staff": "on",
        },
        instance=user,
    )
    assert form.is_valid(), form.errors
    saved = form.save()
    saved.refresh_from_db()
    assert saved.email == "ana@edisa.com"
    assert saved.is_gestor is False
    assert saved.is_staff is False
```

- [ ] **Step 1.2: Ejecutar los tests y verificar que fallan**

Run: `pytest accounts/tests/test_my_account.py -v`
Expected: `ImportError` o `AttributeError` por `ProfileForm` inexistente (todos los tests fallan en colección).

- [ ] **Step 1.3: Implementar `ProfileForm` en `accounts/forms.py`**

Añade al final de `accounts/forms.py`:

```python
from .models import User


class ProfileForm(forms.ModelForm):
    class Meta:
        model = User
        fields = ["name", "dept", "sede", "puesto"]

    def clean_name(self):
        value = (self.cleaned_data.get("name") or "").strip()
        if not value:
            raise forms.ValidationError("El nombre es obligatorio.")
        return value
```

- [ ] **Step 1.4: Ejecutar los tests y verificar que pasan**

Run: `pytest accounts/tests/test_my_account.py -v`
Expected: 5 passed.

- [ ] **Step 1.5: Commit**

```bash
git add accounts/forms.py accounts/tests/test_my_account.py
git commit -m "feat(accounts): ProfileForm para edición de datos propios"
```

---

## Task 2: Ruta `accounts:my_account`

**Files:**
- Modify: `accounts/urls.py`

- [ ] **Step 2.1: Escribir test que comprueba que la URL existe**

Añade al final de `accounts/tests/test_my_account.py`:

```python
def test_my_account_url_resolves():
    assert reverse("accounts:my_account") == "/mi-cuenta/"
```

- [ ] **Step 2.2: Ejecutar y verificar fallo**

Run: `pytest accounts/tests/test_my_account.py::test_my_account_url_resolves -v`
Expected: FAIL — `NoReverseMatch`.

- [ ] **Step 2.3: Añadir la ruta**

Edita `accounts/urls.py` y añade dentro de `urlpatterns`, después de la ruta de `change_password`:

```python
    path("mi-cuenta/", views.MyAccountView.as_view(), name="my_account"),
```

Nota: la view se crea en la Task 3. Este paso provocará `AttributeError` al importar el módulo. Continúa: la siguiente sub-step añade una view mínima placeholder.

- [ ] **Step 2.4: Stub mínimo de `MyAccountView` para que el import resuelva**

Añade al final de `accounts/views.py`:

```python
class MyAccountView(LoginRequiredMixin, View):
    login_url = reverse_lazy("accounts:login")

    def get(self, request):
        return render(request, "accounts/my_account.html", {})
```

(`LoginRequiredMixin`, `View`, `render`, `reverse_lazy` ya están importados arriba en el archivo.)

- [ ] **Step 2.5: Ejecutar y verificar que el test de URL pasa**

Run: `pytest accounts/tests/test_my_account.py::test_my_account_url_resolves -v`
Expected: PASS.

- [ ] **Step 2.6: Commit**

```bash
git add accounts/urls.py accounts/views.py accounts/tests/test_my_account.py
git commit -m "feat(accounts): ruta y stub de MyAccountView"
```

---

## Task 3: Template `my_account.html` (render GET)

**Files:**
- Create: `templates/accounts/my_account.html`

- [ ] **Step 3.1: Escribir test de rendering**

Añade a `accounts/tests/test_my_account.py`:

```python
@pytest.mark.django_db
def test_my_account_redirects_anonymous(client):
    r = client.get(reverse("accounts:my_account"))
    assert r.status_code == 302
    # accounts.urls está montado en "/" en porra26/urls.py, así que login es "/"
    assert "next=/mi-cuenta/" in r.url


@pytest.mark.django_db
def test_my_account_get_renders_for_authenticated_user(client):
    user = UserFactory(email="ana@edisa.com", name="Ana", dept="gestion", sede="vigo", puesto="desarrollo")
    client.force_login(user)
    r = client.get(reverse("accounts:my_account"))
    assert r.status_code == 200
    body = r.content.decode()
    assert "ana@edisa.com" in body
    assert "Datos personales" in body
    assert "Seguridad" in body
    assert "Preferencias" in body
    assert 'name="action" value="profile"' in body
    assert 'name="action" value="password"' in body
```

- [ ] **Step 3.2: Ejecutar y verificar fallo**

Run: `pytest accounts/tests/test_my_account.py -k my_account_get -v`
Expected: FAIL — `TemplateDoesNotExist: accounts/my_account.html`.

- [ ] **Step 3.3: Crear el template**

Crea `templates/accounts/my_account.html`:

```django
{% extends "base.html" %}
{% block title %}Mi cuenta · PORRA 26{% endblock %}
{% block main %}
<section class="stagger" style="max-width:720px;margin:6vh auto;display:flex;flex-direction:column;gap:20px">

  <header>
    <h1 class="display" style="font-size:28px;margin:0">Mi cuenta</h1>
    <p style="color:var(--text-dim);margin:6px 0 0">Gestiona tus datos, contraseña y preferencias.</p>
  </header>

  <div class="glass" style="padding:24px;border-radius:24px">
    <h2 class="display" style="font-size:18px;margin:0 0 14px">Datos personales</h2>
    <div class="field" style="margin-top:6px">
      <label>Correo</label>
      <input type="email" value="{{ user.email }}" disabled style="opacity:.7;cursor:not-allowed">
      <p style="font-size:12px;color:var(--text-faint);margin:6px 0 0">Para cambiar el correo, contacta con un gestor.</p>
    </div>
    <form method="post" novalidate style="margin-top:8px">
      {% csrf_token %}
      <input type="hidden" name="action" value="profile">
      {% for f in profile_form %}
        <div class="field" style="margin-top:14px">
          <label>{{ f.label }}</label>
          {{ f }}
          {% for e in f.errors %}<p style="color:var(--c-red);font-size:12px;margin:4px 0 0">{{ e }}</p>{% endfor %}
        </div>
      {% endfor %}
      {% if profile_form.non_field_errors %}<p style="color:var(--c-red)">{{ profile_form.non_field_errors|join:' ' }}</p>{% endif %}
      <button type="submit" class="btn btn-primary" style="margin-top:18px">Guardar cambios</button>
    </form>
  </div>

  <div class="glass" style="padding:24px;border-radius:24px">
    <h2 class="display" style="font-size:18px;margin:0 0 14px">Seguridad</h2>
    <form method="post" novalidate>
      {% csrf_token %}
      <input type="hidden" name="action" value="password">
      {% for f in password_form %}
        <div class="field" style="margin-top:14px">
          <label>{{ f.label }}</label>
          {{ f }}
          {% for e in f.errors %}<p style="color:var(--c-red);font-size:12px;margin:4px 0 0">{{ e }}</p>{% endfor %}
        </div>
      {% endfor %}
      {% if password_form.non_field_errors %}<p style="color:var(--c-red)">{{ password_form.non_field_errors|join:' ' }}</p>{% endif %}
      <button type="submit" class="btn btn-primary" style="margin-top:18px">Actualizar contraseña</button>
    </form>
  </div>

  <div class="glass" style="padding:24px;border-radius:24px">
    <h2 class="display" style="font-size:18px;margin:0 0 14px">Preferencias</h2>
    <div style="display:flex;align-items:center;justify-content:space-between;gap:16px">
      <div>
        <div style="font-weight:600">Tema</div>
        <div style="font-size:12px;color:var(--text-faint)">Cambia entre claro y oscuro. La preferencia se guarda en este navegador.</div>
      </div>
      <button class="btn btn-ghost" data-theme-toggle style="width:44px;height:44px;padding:0;border-radius:12px" title="Cambiar tema">☀︎</button>
    </div>
  </div>

</section>
{% endblock %}
```

- [ ] **Step 3.4: Pasar los forms desde la view en `GET`**

Reemplaza el cuerpo del `get` de `MyAccountView` en `accounts/views.py`:

```python
    def get(self, request):
        return render(
            request,
            "accounts/my_account.html",
            {
                "profile_form": ProfileForm(instance=request.user),
                "password_form": ChangePasswordForm(request.user),
            },
        )
```

Asegúrate de que el archivo tiene este import al principio:

```python
from .forms import ChangePasswordForm, LoginForm, ProfileForm
```

- [ ] **Step 3.5: Ejecutar tests de rendering**

Run: `pytest accounts/tests/test_my_account.py -k my_account_get -v`
Expected: PASS (2 tests).

También: `pytest accounts/tests/test_my_account.py::test_my_account_redirects_anonymous -v` → PASS.

- [ ] **Step 3.6: Commit**

```bash
git add templates/accounts/my_account.html accounts/views.py accounts/tests/test_my_account.py
git commit -m "feat(accounts): plantilla Mi cuenta y render GET"
```

---

## Task 4: Edición de datos (POST action=profile)

**Files:**
- Modify: `accounts/views.py`

- [ ] **Step 4.1: Escribir tests del POST de datos**

Añade a `accounts/tests/test_my_account.py`:

```python
@pytest.mark.django_db
def test_profile_post_updates_editable_fields(client):
    user = UserFactory(name="Ana", dept="", sede="", puesto="")
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "profile",
            "name": "Ana López",
            "dept": "gestion",
            "sede": "vigo",
            "puesto": "desarrollo",
        },
    )
    assert r.status_code == 302
    assert r.url == reverse("accounts:my_account")
    user.refresh_from_db()
    assert user.name == "Ana López"
    assert user.dept == "gestion"
    assert user.sede == "vigo"
    assert user.puesto == "desarrollo"


@pytest.mark.django_db
def test_profile_post_ignores_email_and_role(client):
    user = UserFactory(email="ana@edisa.com", is_gestor=False, is_staff=False)
    client.force_login(user)
    client.post(
        reverse("accounts:my_account"),
        {
            "action": "profile",
            "name": "Ana",
            "dept": "gestion",
            "sede": "vigo",
            "puesto": "desarrollo",
            "email": "otro@edisa.com",
            "is_gestor": "on",
            "is_staff": "on",
        },
    )
    user.refresh_from_db()
    assert user.email == "ana@edisa.com"
    assert user.is_gestor is False
    assert user.is_staff is False


@pytest.mark.django_db
def test_profile_post_invalid_choice_keeps_db_intact(client):
    user = UserFactory(name="Ana", dept="gestion", sede="vigo", puesto="desarrollo")
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "profile",
            "name": "Ana",
            "dept": "INEXISTENTE",
            "sede": "vigo",
            "puesto": "desarrollo",
        },
    )
    assert r.status_code == 200
    user.refresh_from_db()
    assert user.dept == "gestion"
```

- [ ] **Step 4.2: Ejecutar y verificar fallo**

Run: `pytest accounts/tests/test_my_account.py -k profile_post -v`
Expected: FAIL — el `post` actual no está implementado y devuelve 405 Method Not Allowed.

- [ ] **Step 4.3: Implementar el POST con dispatch por `action`**

Reemplaza la `MyAccountView` completa en `accounts/views.py` por:

```python
class MyAccountView(LoginRequiredMixin, View):
    login_url = reverse_lazy("accounts:login")
    template_name = "accounts/my_account.html"

    def _render(self, request, profile_form=None, password_form=None, status=200):
        return render(
            request,
            self.template_name,
            {
                "profile_form": profile_form or ProfileForm(instance=request.user),
                "password_form": password_form or ChangePasswordForm(request.user),
            },
            status=status,
        )

    def get(self, request):
        return self._render(request)

    def post(self, request):
        action = request.POST.get("action")
        if action == "profile":
            return self._post_profile(request)
        if action == "password":
            return self._post_password(request)
        return HttpResponseBadRequest("acción no válida")

    def _post_profile(self, request):
        form = ProfileForm(request.POST, instance=request.user)
        if not form.is_valid():
            return self._render(request, profile_form=form)
        form.save()
        messages.success(request, "Datos actualizados.")
        return redirect("accounts:my_account")

    def _post_password(self, request):
        form = ChangePasswordForm(request.user, request.POST)
        if not form.is_valid():
            return self._render(request, password_form=form)
        request.user.set_password(form.cleaned_data["new1"])
        request.user.must_change_password = False
        request.user.save(update_fields=["password", "must_change_password"])
        update_session_auth_hash(request, request.user)
        messages.success(request, "Contraseña actualizada.")
        return redirect("accounts:my_account")
```

Y añade/ajusta los imports al principio de `accounts/views.py`:

```python
from django.contrib import messages
from django.contrib.auth import login, logout, update_session_auth_hash
from django.contrib.auth.mixins import LoginRequiredMixin
from django.http import HttpResponseBadRequest
from django.shortcuts import redirect, render
from django.urls import reverse_lazy
from django.views import View

from .forms import ChangePasswordForm, LoginForm, ProfileForm
```

- [ ] **Step 4.4: Ejecutar tests del POST de profile**

Run: `pytest accounts/tests/test_my_account.py -k profile_post -v`
Expected: 3 passed.

- [ ] **Step 4.5: Commit**

```bash
git add accounts/views.py accounts/tests/test_my_account.py
git commit -m "feat(accounts): POST de Mi cuenta para editar datos"
```

---

## Task 5: Cambio de contraseña (POST action=password)

**Files:**
- Modify: nada nuevo (la view ya implementa `_post_password`); se añaden solo tests.

- [ ] **Step 5.1: Escribir tests del POST de contraseña**

Añade a `accounts/tests/test_my_account.py`:

```python
@pytest.mark.django_db
def test_password_post_changes_password_and_keeps_session(client):
    user = UserFactory(email="ana@edisa.com")
    user.set_password("Vieja12345!")
    user.save()
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "password",
            "current": "Vieja12345!",
            "new1": "NuevaSegura1",
            "new2": "NuevaSegura1",
        },
    )
    assert r.status_code == 302
    user.refresh_from_db()
    assert user.check_password("NuevaSegura1")
    # sesión sigue viva
    r2 = client.get(reverse("accounts:my_account"))
    assert r2.status_code == 200


@pytest.mark.django_db
def test_password_post_wrong_current_keeps_password(client):
    user = UserFactory()
    user.set_password("Correcta1234")
    user.save()
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "password",
            "current": "Incorrecta1234",
            "new1": "NuevaSegura1",
            "new2": "NuevaSegura1",
        },
    )
    assert r.status_code == 200
    user.refresh_from_db()
    assert user.check_password("Correcta1234")


@pytest.mark.django_db
def test_password_post_mismatch_shows_error(client):
    user = UserFactory()
    user.set_password("Correcta1234")
    user.save()
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "password",
            "current": "Correcta1234",
            "new1": "NuevaSegura1",
            "new2": "OtraDistinta1",
        },
    )
    assert r.status_code == 200
    assert b"no coinciden" in r.content


@pytest.mark.django_db
def test_password_post_weak_rejected(client):
    user = UserFactory()
    user.set_password("Correcta1234")
    user.save()
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "password",
            "current": "Correcta1234",
            "new1": "todominusculas",
            "new2": "todominusculas",
        },
    )
    assert r.status_code == 200
    user.refresh_from_db()
    assert user.check_password("Correcta1234")


@pytest.mark.django_db
def test_password_post_keeps_must_change_flag_false(client):
    """El cambio normal desde /mi-cuenta/ ocurre cuando must_change_password ya
    es False (el middleware redirige si fuera True). Verificamos que tras el
    cambio el flag sigue siendo False."""
    user = UserFactory(must_change_password=False)
    user.set_password("Inicial12345")
    user.save()
    client.force_login(user)
    r = client.post(
        reverse("accounts:my_account"),
        {
            "action": "password",
            "current": "Inicial12345",
            "new1": "NuevaSegura1",
            "new2": "NuevaSegura1",
        },
    )
    assert r.status_code == 302
    user.refresh_from_db()
    assert user.must_change_password is False
```

- [ ] **Step 5.2: Ejecutar y verificar que pasan**

Run: `pytest accounts/tests/test_my_account.py -k password_post -v`
Expected: 5 passed.

- [ ] **Step 5.3: Commit**

```bash
git add accounts/tests/test_my_account.py
git commit -m "test(accounts): cobertura del cambio de contraseña desde Mi cuenta"
```

---

## Task 6: Despacho por `action` (400 si falta o es desconocido)

**Files:**
- Modify: nada nuevo (la view ya devuelve 400); se añaden tests.

- [ ] **Step 6.1: Escribir tests del dispatch**

Añade a `accounts/tests/test_my_account.py`:

```python
@pytest.mark.django_db
def test_post_without_action_returns_400(client):
    user = UserFactory()
    client.force_login(user)
    r = client.post(reverse("accounts:my_account"), {"name": "Ana"})
    assert r.status_code == 400


@pytest.mark.django_db
def test_post_unknown_action_returns_400(client):
    user = UserFactory()
    client.force_login(user)
    r = client.post(reverse("accounts:my_account"), {"action": "explotame"})
    assert r.status_code == 400
```

- [ ] **Step 6.2: Ejecutar y verificar que pasan**

Run: `pytest accounts/tests/test_my_account.py -k "without_action or unknown_action" -v`
Expected: 2 passed.

- [ ] **Step 6.3: Commit**

```bash
git add accounts/tests/test_my_account.py
git commit -m "test(accounts): dispatch por action en MyAccountView"
```

---

## Task 7: Auditoría con AuditLog

**Files:**
- Modify: `accounts/views.py`

- [ ] **Step 7.1: Escribir tests de AuditLog**

Añade a `accounts/tests/test_my_account.py`:

```python
@pytest.mark.django_db
def test_profile_post_writes_audit_log_when_changed(client):
    user = UserFactory(name="Ana", dept="gestion", sede="vigo", puesto="desarrollo")
    client.force_login(user)
    client.post(
        reverse("accounts:my_account"),
        {
            "action": "profile",
            "name": "Ana López",
            "dept": "gestion",
            "sede": "vigo",
            "puesto": "desarrollo",
        },
    )
    log = AuditLog.objects.filter(action="profile.update").first()
    assert log is not None
    assert log.actor_id == user.id
    assert log.target_type == "user"
    assert log.target_id == str(user.id)
    assert log.payload == {"changed": ["name"]}


@pytest.mark.django_db
def test_profile_post_no_audit_when_nothing_changed(client):
    user = UserFactory(name="Ana", dept="gestion", sede="vigo", puesto="desarrollo")
    client.force_login(user)
    client.post(
        reverse("accounts:my_account"),
        {
            "action": "profile",
            "name": "Ana",
            "dept": "gestion",
            "sede": "vigo",
            "puesto": "desarrollo",
        },
    )
    assert AuditLog.objects.filter(action="profile.update").count() == 0


@pytest.mark.django_db
def test_password_change_writes_audit_log(client):
    user = UserFactory()
    user.set_password("Correcta1234")
    user.save()
    client.force_login(user)
    client.post(
        reverse("accounts:my_account"),
        {
            "action": "password",
            "current": "Correcta1234",
            "new1": "NuevaSegura1",
            "new2": "NuevaSegura1",
        },
    )
    log = AuditLog.objects.filter(action="password.change").first()
    assert log is not None
    assert log.actor_id == user.id
    assert log.target_type == "user"
    assert log.target_id == str(user.id)
    assert log.payload == {}
```

- [ ] **Step 7.2: Ejecutar y verificar fallo**

Run: `pytest accounts/tests/test_my_account.py -k audit -v`
Expected: FAIL — no se está creando ningún `AuditLog`.

- [ ] **Step 7.3: Implementar la auditoría en la view**

En `accounts/views.py`, añade el import:

```python
from .models import AuditLog
```

Modifica `_post_profile` para auditar solo cuando hay cambios:

```python
    def _post_profile(self, request):
        form = ProfileForm(request.POST, instance=request.user)
        if not form.is_valid():
            return self._render(request, profile_form=form)
        changed = list(form.changed_data)
        form.save()
        if changed:
            AuditLog.objects.create(
                actor=request.user,
                action="profile.update",
                target_type="user",
                target_id=str(request.user.id),
                payload={"changed": changed},
            )
        messages.success(request, "Datos actualizados.")
        return redirect("accounts:my_account")
```

Modifica `_post_password` para registrar el cambio (sin payload):

```python
    def _post_password(self, request):
        form = ChangePasswordForm(request.user, request.POST)
        if not form.is_valid():
            return self._render(request, password_form=form)
        request.user.set_password(form.cleaned_data["new1"])
        request.user.must_change_password = False
        request.user.save(update_fields=["password", "must_change_password"])
        update_session_auth_hash(request, request.user)
        AuditLog.objects.create(
            actor=request.user,
            action="password.change",
            target_type="user",
            target_id=str(request.user.id),
        )
        messages.success(request, "Contraseña actualizada.")
        return redirect("accounts:my_account")
```

- [ ] **Step 7.4: Ejecutar tests**

Run: `pytest accounts/tests/test_my_account.py -k audit -v`
Expected: 3 passed.

- [ ] **Step 7.5: Commit**

```bash
git add accounts/views.py accounts/tests/test_my_account.py
git commit -m "feat(accounts): auditoría de cambios en Mi cuenta"
```

---

## Task 8: Enlace desde el topbar

**Files:**
- Modify: `templates/partials/_topbar.html`

- [ ] **Step 8.1: Escribir test del enlace**

Añade a `accounts/tests/test_my_account.py`:

```python
@pytest.mark.django_db
def test_topbar_avatar_links_to_my_account(client):
    user = UserFactory()
    client.force_login(user)
    # Usamos la propia /mi-cuenta/ — también renderiza el topbar y no depende
    # de que existan datos del dashboard de competición.
    r = client.get(reverse("accounts:my_account"))
    assert r.status_code == 200
    body = r.content.decode()
    assert 'href="/mi-cuenta/"' in body
```

- [ ] **Step 8.2: Ejecutar y verificar fallo**

Run: `pytest accounts/tests/test_my_account.py::test_topbar_avatar_links_to_my_account -v`
Expected: FAIL — no se encuentra el href.

- [ ] **Step 8.3: Editar el topbar**

Abre `templates/partials/_topbar.html`. Localiza el bloque del avatar (las líneas 32–37 aproximadamente):

```django
<div style="display:flex;align-items:center;gap:9px;padding-left:6px">
  <span class="avatar" data-name="{{ user.name }}">{{ user.initials }}</span>
  <div style="line-height:1.2">
    <div style="font-size:13px;font-weight:700">{{ user.name }}</div>
    <div class="mono" style="font-size:10.5px;color:var(--text-faint);text-transform:capitalize">{{ user.role }}</div>
  </div>
```

Envuelve **únicamente** el avatar y el bloque de nombre/rol en un `<a>` (el `<form>` de logout se queda como hermano, fuera del `<a>`):

```django
<div style="display:flex;align-items:center;gap:9px;padding-left:6px">
  <a href="{% url 'accounts:my_account' %}" style="display:flex;align-items:center;gap:9px;text-decoration:none;color:inherit;border-radius:11px;padding:4px 6px{% if request.resolver_match.namespace == 'accounts' and request.resolver_match.url_name == 'my_account' %};background:var(--surface-hi){% endif %}" title="Mi cuenta">
    <span class="avatar" data-name="{{ user.name }}">{{ user.initials }}</span>
    <div style="line-height:1.2">
      <div style="font-size:13px;font-weight:700">{{ user.name }}</div>
      <div class="mono" style="font-size:10.5px;color:var(--text-faint);text-transform:capitalize">{{ user.role }}</div>
    </div>
  </a>
  <form method="post" action="{% url 'accounts:logout' %}" style="display:inline">
    {% csrf_token %}
    <button class="btn btn-ghost" type="submit" style="width:38px;height:38px;padding:0;border-radius:11px;margin-left:4px" title="Salir">
      {% icon "logout" width=16 height=16 %}
    </button>
  </form>
</div>
```

Nota: `{{ user.role }}` aparece tal cual estaba (legacy del split is_jugador/is_gestor de la sesión paralela). No es alcance de este plan corregirlo.

- [ ] **Step 8.4: Ejecutar test**

Run: `pytest accounts/tests/test_my_account.py::test_topbar_avatar_links_to_my_account -v`
Expected: PASS.

- [ ] **Step 8.5: Commit**

```bash
git add templates/partials/_topbar.html accounts/tests/test_my_account.py
git commit -m "feat(topbar): enlace desde el avatar a Mi cuenta"
```

---

## Task 9: Verificación final

- [ ] **Step 9.1: Suite completa**

Run: `pytest -q`
Expected: todos los tests pasan (los nuevos + los preexistentes).

- [ ] **Step 9.2: Lint**

Run: `ruff check . && ruff format --check .`
Expected: sin errores. Si `ruff format --check` falla, ejecuta `ruff format .` y haz un commit `chore: ruff format` separado.

- [ ] **Step 9.3: Cobertura**

Run: `pytest --cov=accounts --cov-report=term-missing -q`
Expected: la cobertura global del paquete `accounts/` está por encima del 70 % del CI, y `accounts/views.py:MyAccountView` aparece cubierta.

- [ ] **Step 9.4: Smoke manual**

Levanta el servidor en local y verifica:

```bash
python manage.py runserver
```

1. Login con un usuario existente (o crea uno con `python manage.py createsuperuser` + `must_change_password=False`).
2. Click en el avatar del topbar → navega a `/mi-cuenta/`.
3. Edita el nombre y un select → guarda → ver toast "Datos actualizados." y los cambios persisten al recargar.
4. Intenta cambiar la contraseña con la actual mal → ve error y la sesión sigue activa.
5. Cambia la contraseña correctamente → ve toast "Contraseña actualizada." y al recargar sigue logueado.
6. Toggle de tema en la tarjeta Preferencias → cambia el tema globalmente y persiste al recargar (localStorage).

- [ ] **Step 9.5: Commit final si quedó algo de formato pendiente**

```bash
git status
# Si hay cambios sin commitear, decide qué hacer; idealmente no debería haberlos.
```

---

## Notas para el ejecutor

- **No tocar** `accounts/middleware.py`, el flujo `/cambiar-password/`, ni nada relacionado con `must_change_password` más allá de lo descrito en Task 4/5.
- **No tocar** `accounts/mixins.py` ni el `{% if user.role == 'gestor' %}` del topbar (legacy en transición; fuera de alcance).
- **No añadir** nuevos campos al modelo `User` ni migraciones nuevas.
- **No introducir** JS adicional; el toggle de tema usa el selector `data-theme-toggle` que ya gestiona `static/js/theme.js`.
- El `_render` con `status=200` es deliberado para que los forms con errores se muestren sin redirigir (patrón POST-falla-200, POST-éxito-redirect).
