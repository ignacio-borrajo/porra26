# Arreglo de cruces R32 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corregir fechas/horas y orden de los 16 cruces de Dieciseisavos (R32), hacer que sus equipos los asigne siempre un administrador (sin auto-relleno), y dar un comando para resetear lo creado por error.

**Architecture:** Se añade `Match.bracket_order` como orden explícito de los cruces. El fixture (`world_cup_2026.json`) sigue siendo la fuente de verdad de fechas/orden; el comando `seed` lo aplica de forma idempotente. La vista de Competición ordena R32 por `bracket_order` y agrupa parejas por posición. La propagación automática se desactiva solo para R32. Un comando `reset_r32_crosses` limpia el estado KO en producción.

**Tech Stack:** Django 5.1, pytest + factory_boy, SQLite (test) / Postgres (prod).

## Global Constraints

- Idioma de UI y textos: **español de España**.
- Zona horaria: `TIME_ZONE = "Europe/Madrid"`, `USE_TZ = True`. Las horas de la tabla son hora local de Madrid; se guardan en UTC (CEST = −2h).
- SQL/ORM: usar el ORM de Django (sin SQL crudo).
- Comando de tests: `/Users/ignacioborrajo/Documents/GitHub/apuestas-interna/.venv/bin/python -m pytest` (settings de test vía `pyproject.toml`).
- No tocar los slots `home_slot`/`away_slot` ni los `WM…` de octavos. Solo `kickoff` y `bracket_order`.
- No cambiar emparejamientos del cuadro (qué ganador va a qué octavo).

### Tabla canónica R32 (orden, código, kickoff UTC)

| Orden | Código | kickoff UTC |
|------|------|------|
| 1  | M74 | `2026-06-29T20:30:00Z` |
| 2  | M77 | `2026-06-30T21:00:00Z` |
| 3  | M73 | `2026-06-28T19:00:00Z` |
| 4  | M75 | `2026-06-30T01:00:00Z` |
| 5  | M83 | `2026-07-02T23:00:00Z` |
| 6  | M84 | `2026-07-02T19:00:00Z` |
| 7  | M81 | `2026-07-02T00:00:00Z` |
| 8  | M82 | `2026-07-01T20:00:00Z` |
| 9  | M76 | `2026-06-29T17:00:00Z` |
| 10 | M78 | `2026-06-30T17:00:00Z` |
| 11 | M79 | `2026-07-01T01:00:00Z` |
| 12 | M80 | `2026-07-01T16:00:00Z` |
| 13 | M86 | `2026-07-03T22:00:00Z` |
| 14 | M88 | `2026-07-03T18:00:00Z` |
| 15 | M85 | `2026-07-03T03:00:00Z` |
| 16 | M87 | `2026-07-04T01:30:00Z` |

---

### Task 1: Campo `bracket_order` en el modelo + migración

**Files:**
- Modify: `competition/models.py` (clase `Match`, tras `bracket_code`, línea ~52)
- Create: `competition/migrations/00NN_match_bracket_order.py` (autogenerada)

**Interfaces:**
- Produces: `Match.bracket_order: int | None`.

- [ ] **Step 1: Añadir el campo**

En `competition/models.py`, justo después de `bracket_code = ...`:

```python
    bracket_order = models.PositiveSmallIntegerField(null=True, blank=True)
```

- [ ] **Step 2: Generar la migración**

Run: `.venv/bin/python manage.py makemigrations competition`
Expected: crea `competition/migrations/00NN_match_bracket_order.py` con `AddField`.

- [ ] **Step 3: Verificar que aplica**

Run: `.venv/bin/python manage.py migrate competition --plan`
Expected: lista la nueva migración sin errores.

- [ ] **Step 4: Commit**

```bash
git add competition/models.py competition/migrations/
git commit -m "feat(competition): campo bracket_order en Match"
```

---

### Task 2: Corregir fixture R32 (kickoff + bracket_order)

**Files:**
- Modify: `fixtures/world_cup_2026.json` (entradas con `bracket_code` M73–M88)
- Test: `competition/tests/test_fixture_r32.py` (nuevo)

**Interfaces:**
- Consumes: `Match.bracket_order` (Task 1).
- Produces: fixture con `kickoff` correcto y `bracket_order` 1–16 en M73–M88.

- [ ] **Step 1: Escribir el test que valida el fixture**

Create `competition/tests/test_fixture_r32.py`:

```python
import json
from pathlib import Path

FIXTURE = Path(__file__).resolve().parents[2] / "fixtures" / "world_cup_2026.json"

EXPECTED = {
    "M74": ("2026-06-29T20:30:00Z", 1),
    "M77": ("2026-06-30T21:00:00Z", 2),
    "M73": ("2026-06-28T19:00:00Z", 3),
    "M75": ("2026-06-30T01:00:00Z", 4),
    "M83": ("2026-07-02T23:00:00Z", 5),
    "M84": ("2026-07-02T19:00:00Z", 6),
    "M81": ("2026-07-02T00:00:00Z", 7),
    "M82": ("2026-07-01T20:00:00Z", 8),
    "M76": ("2026-06-29T17:00:00Z", 9),
    "M78": ("2026-06-30T17:00:00Z", 10),
    "M79": ("2026-07-01T01:00:00Z", 11),
    "M80": ("2026-07-01T16:00:00Z", 12),
    "M86": ("2026-07-03T22:00:00Z", 13),
    "M88": ("2026-07-03T18:00:00Z", 14),
    "M85": ("2026-07-03T03:00:00Z", 15),
    "M87": ("2026-07-04T01:30:00Z", 16),
}


def test_r32_fixture_kickoffs_and_order():
    data = json.loads(FIXTURE.read_text(encoding="utf-8"))
    by_code = {
        e["fields"]["bracket_code"]: e["fields"]
        for e in data
        if e["fields"].get("bracket_code") in EXPECTED
    }
    assert set(by_code) == set(EXPECTED)
    for code, (kickoff, order) in EXPECTED.items():
        assert by_code[code]["kickoff"] == kickoff, code
        assert by_code[code]["bracket_order"] == order, code
```

- [ ] **Step 2: Ejecutar el test (debe fallar)**

Run: `.venv/bin/python -m pytest competition/tests/test_fixture_r32.py -q`
Expected: FAIL (falta `bracket_order` y kickoffs viejos).

- [ ] **Step 3: Editar el fixture**

En `fixtures/world_cup_2026.json`, para cada entrada M73–M88: poner el `kickoff` de la tabla y añadir `"bracket_order": <n>` dentro de `fields`. (Editar las 16 entradas.)

- [ ] **Step 4: Ejecutar el test (debe pasar)**

Run: `.venv/bin/python -m pytest competition/tests/test_fixture_r32.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add fixtures/world_cup_2026.json competition/tests/test_fixture_r32.py
git commit -m "fix(competition): fechas/horas y orden correctos de los cruces R32"
```

---

### Task 3: `seed` aplica `bracket_order` a partidos existentes

**Files:**
- Modify: `competition/management/commands/seed_world_cup_2026.py` (rama KO, líneas ~78-114)
- Test: `competition/tests/test_seed_command.py` (añadir test)

**Interfaces:**
- Consumes: fixture con `bracket_order` (Task 2), `Match.bracket_order` (Task 1).
- Produces: tras `seed`, los R32 tienen `bracket_order` correcto y kickoff refrescado, sin pisar `home`/`away`.

- [ ] **Step 1: Escribir el test**

Añadir a `competition/tests/test_seed_command.py`:

```python
@pytest.mark.django_db
def test_seed_sets_bracket_order_on_r32():
    call_command("seed_world_cup_2026")
    m74 = Match.objects.get(bracket_code="M74")
    assert m74.bracket_order == 1
    m73 = Match.objects.get(bracket_code="M73")
    assert m73.bracket_order == 3


@pytest.mark.django_db
def test_seed_updates_bracket_order_without_touching_teams():
    call_command("seed_world_cup_2026")
    m = Match.objects.get(bracket_code="M73")
    home = TeamFactory(code="ZZ1")
    m.home = home
    m.bracket_order = None
    m.save(update_fields=["home", "bracket_order"])
    call_command("seed_world_cup_2026")
    m.refresh_from_db()
    assert m.bracket_order == 3       # reaplicado
    assert m.home_id == "ZZ1"         # no se pisa el equipo asignado
```

- [ ] **Step 2: Ejecutar (debe fallar)**

Run: `.venv/bin/python -m pytest competition/tests/test_seed_command.py -q`
Expected: FAIL (`bracket_order` es None).

- [ ] **Step 3: Implementar**

En la rama KO de `handle`: en la CREACIÓN añadir `bracket_order=f.get("bracket_order")` al `Match.objects.create(...)`. En la ACTUALIZACIÓN, tras el bloque de slots y antes de `if changed:`:

```python
                    new_order = f.get("bracket_order")
                    if existing.bracket_order != new_order:
                        existing.bracket_order = new_order
                        changed = True
```

- [ ] **Step 4: Ejecutar (debe pasar)**

Run: `.venv/bin/python -m pytest competition/tests/test_seed_command.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add competition/management/commands/seed_world_cup_2026.py competition/tests/test_seed_command.py
git commit -m "feat(competition): seed aplica bracket_order a los cruces R32"
```

---

### Task 4: Sin auto-relleno de equipos en R32

**Files:**
- Modify: `competition/services/bracket.py` (`propagate_after_match`, líneas 74-97)
- Test: `competition/tests/test_bracket_no_autofill_r32.py` (nuevo)

**Interfaces:**
- Consumes: `propagate_after_match(match) -> list[Match]`.
- Produces: R32 nunca se autorrellena; r16+ sí (desde `WM…`).

- [ ] **Step 1: Escribir el test**

Create `competition/tests/test_bracket_no_autofill_r32.py`:

```python
import pytest

from competition.models import Match
from competition.services.bracket import propagate_after_match
from competition.services.resolve import resolve_match
from competition.tests.factories import MatchFactory, RoundFactory, TeamFactory


@pytest.fixture(autouse=True)
def _rounds(db):
    RoundFactory(id="groups", points=3, order=1)
    RoundFactory(id="r32", points=5, label="Dieciseisavos", short="R32", order=2)
    RoundFactory(id="r16", points=7, label="Octavos", short="R16", order=3)


@pytest.mark.django_db
def test_r32_no_autofill_from_group_standings(admin_user):
    a, b, c, d = [TeamFactory() for _ in range(4)]
    # Grupo A de 1 partido ya resuelto: standings calculables.
    MatchFactory(round_id="groups", group="A", matchday=1, home=a, away=b,
                 result_home=1, result_away=0)
    # Cruce R32 que apunta a "1A".
    r32 = MatchFactory(round_id="r32", group="Dieciseisavos", matchday=None,
                       home=None, away=None, home_slot="1A", away_slot="2A",
                       bracket_code="M73")
    propagate_after_match(r32)
    r32.refresh_from_db()
    assert r32.home_id is None and r32.away_id is None  # NO autorrelleno


@pytest.mark.django_db
def test_r16_autofills_from_r32_winner(admin_user):
    a, b = TeamFactory(), TeamFactory()
    r32 = MatchFactory(round_id="r32", group="Dieciseisavos", matchday=None,
                       home=a, away=b, home_slot="1A", away_slot="2A",
                       bracket_code="M73")
    r16 = MatchFactory(round_id="r16", group="Octavos", matchday=None,
                       home=None, away=None, home_slot="WM73", away_slot="WM75",
                       bracket_code="M89")
    resolve_match(r32, home=2, away=1, actor=admin_user)
    r16.refresh_from_db()
    assert r16.home_id == a.code  # ganador de M73 propagado a octavos
```

- [ ] **Step 2: Ejecutar (debe fallar)**

Run: `.venv/bin/python -m pytest competition/tests/test_bracket_no_autofill_r32.py -q`
Expected: FAIL en `test_r32_no_autofill...` (hoy sí rellena).

- [ ] **Step 3: Implementar**

En `propagate_after_match`, excluir R32 del queryset de pendientes:

```python
    pending = (
        Match.objects.filter(home__isnull=True).exclude(home_slot="")
        | Match.objects.filter(away__isnull=True).exclude(away_slot="")
    ).distinct().exclude(round_id="r32")
```

- [ ] **Step 4: Ejecutar (debe pasar)**

Run: `.venv/bin/python -m pytest competition/tests/test_bracket_no_autofill_r32.py competition/tests/test_resolve.py -q`
Expected: PASS (incluye regresión de resolve).

- [ ] **Step 5: Commit**

```bash
git add competition/services/bracket.py competition/tests/test_bracket_no_autofill_r32.py
git commit -m "feat(competition): los equipos de R32 ya no se autorrellenan (asignación manual)"
```

---

### Task 5: Orden 1–16 en la vista de Competición + parejas por posición

**Files:**
- Modify: `competition/views.py` (`_group_into_pairs` + bloque `ko_rounds`, líneas 16-28 y 107-140)
- Test: `competition/tests/test_ko_view_order.py` (nuevo)

**Interfaces:**
- Consumes: `Match.bracket_order`.
- Produces: en el contexto de Competición, `ko_rounds` con los R32 ordenados por `bracket_order` y `pairs` en chunks de 2 por posición.

- [ ] **Step 1: Escribir el test**

Create `competition/tests/test_ko_view_order.py`:

```python
import pytest
from django.urls import reverse

from competition.tests.factories import MatchFactory, RoundFactory, TeamFactory


@pytest.fixture(autouse=True)
def _rounds(db):
    for rid, order, label in [("groups", 1, "Fase de grupos"), ("r32", 2, "Dieciseisavos"),
                              ("r16", 3, "Octavos"), ("qf", 4, "Cuartos"),
                              ("sf", 5, "Semifinales"), ("final", 6, "Final")]:
        RoundFactory(id=rid, order=order, label=label, short=rid.upper())


@pytest.mark.django_db
def test_r32_ordered_by_bracket_order(client, django_user_model):
    user = django_user_model.objects.create_user(
        email="j@edisa.com", password="x", name="J")
    for code, order in [("M73", 3), ("M74", 1), ("M77", 2), ("M75", 4)]:
        MatchFactory(round_id="r32", group="Dieciseisavos", matchday=None,
                     home=None, away=None, home_slot="1A", away_slot="2A",
                     bracket_code=code, bracket_order=order)
    client.force_login(user)
    resp = client.get(reverse("competition") + "?round=r32")
    ko = {e["round"].id: e for e in resp.context["ko_rounds"]}["r32"]
    codes = [m.bracket_code for m in ko["matches"]]
    assert codes == ["M74", "M77", "M73", "M75"]
    # parejas por posición: [M74, M77], [M73, M75]
    pair_codes = [[m.bracket_code for m in pair] for pair in ko["pairs"]]
    assert pair_codes == [["M74", "M77"], ["M73", "M75"]]
```

(Confirmar el `name` real de la URL en `competition/urls.py`; si no es `"competition"`, usar el correcto.)

- [ ] **Step 2: Ejecutar (debe fallar)**

Run: `.venv/bin/python -m pytest competition/tests/test_ko_view_order.py -q`
Expected: FAIL (orden actual por feeds/bracket_code).

- [ ] **Step 3: Implementar**

En `competition/views.py`, añadir helper junto a `_group_into_pairs`:

```python
def _chunk_pairs(matches: list) -> list[list]:
    """Agrupa la lista en parejas consecutivas [[m0,m1],[m2,m3],...]."""
    return [matches[i:i + 2] for i in range(0, len(matches), 2)]
```

En el bucle de `ko_rounds` (sustituye el `sorted(...)` y el `pairs`):

```python
            for rid in KO_ROUND_IDS:
                r_obj = rounds_by_id.get(rid)
                if r_obj is None:
                    continue
                rlist = [m for m in ko_matches if m.round_id == rid]
                if rid == "r32":
                    rmatches = sorted(
                        rlist,
                        key=lambda m: (
                            m.bracket_order if m.bracket_order is not None else 9999,
                            m.bracket_code or "",
                        ),
                    )
                    pairs = _chunk_pairs(rmatches)
                else:
                    rmatches = sorted(
                        rlist,
                        key=lambda m: (m.feeds_into_code or "", m.bracket_code or ""),
                    )
                    pairs = _group_into_pairs(rmatches)
                ko_rounds.append(
                    {"round": r_obj, "matches": rmatches, "pairs": pairs}
                )
```

- [ ] **Step 4: Ejecutar (debe pasar)**

Run: `.venv/bin/python -m pytest competition/tests/test_ko_view_order.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add competition/views.py competition/tests/test_ko_view_order.py
git commit -m "feat(competition): R32 se ordena por bracket_order y parejas por posición"
```

---

### Task 6: Comando `reset_r32_crosses`

**Files:**
- Create: `competition/management/commands/reset_r32_crosses.py`
- Test: `competition/tests/test_reset_r32_command.py` (nuevo)

**Interfaces:**
- Consumes: `seed_world_cup_2026` (para reaplicar kickoff/orden) o lectura del fixture; servicios `clear_match_result`.
- Produces: comando `reset_r32_crosses` que deja los KO en `pending_teams`, sin pronósticos, con R32 kickoff/orden correctos.

- [ ] **Step 1: Escribir el test**

Create `competition/tests/test_reset_r32_command.py`:

```python
import pytest
from django.core.management import call_command

from competition.models import Match, Prediction
from competition.tests.factories import (
    MatchFactory, PredictionFactory, RoundFactory, TeamFactory,
)


@pytest.fixture(autouse=True)
def _rounds(db):
    for rid, order in [("groups", 1), ("r32", 2), ("r16", 3),
                       ("qf", 4), ("sf", 5), ("final", 6)]:
        RoundFactory(id=rid, order=order, short=rid.upper())


@pytest.mark.django_db
def test_reset_returns_r32_to_pending_and_clears_predictions():
    call_command("seed_world_cup_2026")
    m = Match.objects.get(bracket_code="M73")
    a, b = TeamFactory(code="AA1"), TeamFactory(code="BB1")
    m.home, m.away = a, b
    m.result_home, m.result_away = 2, 1
    m.save()
    PredictionFactory(match=m, home=2, away=1)

    call_command("reset_r32_crosses")

    m.refresh_from_db()
    assert m.home_id is None and m.away_id is None
    assert m.result_home is None and m.result_away is None
    assert m.status == "pending_teams"
    assert m.kickoff.isoformat().startswith("2026-06-28T19:00")
    assert m.bracket_order == 3
    assert not Prediction.objects.filter(match=m).exists()


@pytest.mark.django_db
def test_reset_clears_downstream_ko_teams():
    call_command("seed_world_cup_2026")
    r16 = Match.objects.get(bracket_code="M89")
    r16.home = TeamFactory(code="CC1")
    r16.save()
    call_command("reset_r32_crosses")
    r16.refresh_from_db()
    assert r16.home_id is None
```

- [ ] **Step 2: Ejecutar (debe fallar)**

Run: `.venv/bin/python -m pytest competition/tests/test_reset_r32_command.py -q`
Expected: FAIL (`Unknown command: 'reset_r32_crosses'`).

- [ ] **Step 3: Implementar el comando**

Create `competition/management/commands/reset_r32_crosses.py`:

```python
"""Resetea los cruces KO a estado pendiente y reaplica fechas/orden de R32.

Pensado para producción cuando un administrador asignó equipos por error:
- Reaplica kickoff + bracket_order de R32 desde el fixture (vía seed).
- Devuelve todos los cruces KO con slots a 'pending_teams': limpia equipos,
  resultados, pronósticos, marcador en vivo e informes asociados.

Idempotente. Ejecutar manualmente tras el deploy:
    python manage.py reset_r32_crosses
"""

from django.core.management import call_command
from django.core.management.base import BaseCommand
from django.db import transaction

from accounts.models import AuditLog
from competition.models import BetsClosingReport, LiveScore, Match, Prediction


class Command(BaseCommand):
    help = "Resetea los cruces KO a pendiente y reaplica fechas/orden de R32."

    @transaction.atomic
    def handle(self, **opts):
        # 1) Reaplicar kickoff + bracket_order desde el fixture (no toca equipos).
        call_command("seed_world_cup_2026")

        # 2) Limpiar estado de todos los cruces KO con slots.
        ko = Match.objects.exclude(round_id="groups").exclude(home_slot="").exclude(
            away_slot=""
        )
        n = 0
        for m in ko:
            Prediction.objects.filter(match=m).delete()
            LiveScore.objects.filter(match=m).delete()
            BetsClosingReport.objects.filter(match=m).delete()
            m.home = None
            m.away = None
            m.result_home = None
            m.result_away = None
            m.finished_at = None
            m.exact_points_applied = None
            m.partial_points_applied = None
            m.save(update_fields=[
                "home", "away", "result_home", "result_away", "finished_at",
                "exact_points_applied", "partial_points_applied",
            ])
            n += 1

        AuditLog.objects.create(
            actor=None,
            action="r32_reset",
            target_type="match",
            target_id="r32",
            payload={"reset_count": n},
        )
        self.stdout.write(self.style.SUCCESS(f"Reseteados {n} cruce(s) KO a pendiente."))
```

(Confirmar que `AuditLog.actor` admite `null`; si no, omitir el registro o pasar un actor de sistema.)

- [ ] **Step 4: Ejecutar (debe pasar)**

Run: `.venv/bin/python -m pytest competition/tests/test_reset_r32_command.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add competition/management/commands/reset_r32_crosses.py competition/tests/test_reset_r32_command.py
git commit -m "feat(competition): comando reset_r32_crosses para resetear cruces KO"
```

---

### Task 7: Actualizar docs

**Files:**
- Modify: `docs/DATA_MODEL.md`

- [ ] **Step 1: Editar la doc**

En la sección de KO/propagación de `docs/DATA_MODEL.md`, indicar que los equipos de los cruces R32 (Dieciseisavos) **los asigna manualmente el gestor** (pantalla "Asignar equipos") y no se autorrellenan al cerrar los grupos; octavos y posteriores sí se propagan desde el ganador del cruce anterior.

- [ ] **Step 2: Commit**

```bash
git add docs/DATA_MODEL.md
git commit -m "docs: asignación manual de equipos en cruces R32"
```

---

### Task 8: Suite completa verde

- [ ] **Step 1: Ejecutar toda la suite**

Run: `.venv/bin/python -m pytest -q`
Expected: 0 failures (≥367 + nuevos tests).

- [ ] **Step 2: Si algo falla, corregir y re-ejecutar.**

---

## Self-Review

- **Cobertura del spec:** modelo (T1), fixture/fechas/orden (T2), seed (T3), sin auto-relleno R32 (T4), orden UI + parejas (T5), reset (T6), docs (T7), suite (T8). ✔
- **Sin placeholders:** dos confirmaciones marcadas explícitamente (nombre de URL en T5, `AuditLog.actor` nullable en T6) — son verificaciones de un valor existente, no contenido pendiente.
- **Consistencia de tipos:** `bracket_order: int | None`; `_chunk_pairs(list) -> list[list]`; `propagate_after_match` mantiene firma.
